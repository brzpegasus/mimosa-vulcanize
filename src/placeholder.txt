

  vulcanize:                      # Configuration for the mimosa-vulcanize module.
    filename: 'components.html'   # The name of the html file to feed to vulcanize,
                                  # relative to mimosaConfig.watch.sourceDir.
    strip: true                   # Whether to strip whitespace and comments from the output.
    inline: true                  # Whether to inline all JS and CSS into the document output.
    csp: true                     # Whether to extract inline scripts to a separate file (<filename>.js).
                                  # If both `inline` and `csp` are enabled, all javascript,
                                  # inline and external, are extracted into a separate JS file.
